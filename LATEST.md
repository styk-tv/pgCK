<!--
  This file is auto-generated by .github/workflows/update-latest-md.yml after a
  successful release.yml / publish-pgck-web.yml run AND after SLSA Build
  Provenance v1 attestations verify against the GHCR digests it's about to
  publish. Do NOT edit by hand — manual edits are reverted on the next
  workflow run. See PROVENANCE.md for the policy.
-->

# pgCK — latest published artifacts

> **No attested release published yet.**
>
> The build-provenance pipeline (`actions/attest-build-provenance@v1` →
> `gh attestation verify`) was wired up on 2026-05-28. Releases that
> predate that commit live in GHCR but do not yet carry attestations,
> so this file does not advertise them as the "latest head" — that would
> violate the very rule the pipeline exists to enforce.
>
> This file will be populated by the `update-latest-md` workflow on the
> first successful `release.yml` or `publish-pgck-web.yml` run after the
> attestation step takes effect. Until then, browse GHCR directly:
>
> - Extension: <https://github.com/styk-tv/pgCK/pkgs/container/pgck>
> - pgck-web : <https://github.com/styk-tv/pgCK/pkgs/container/pgck-web>
>
> See [`PROVENANCE.md`](./PROVENANCE.md) for the verification policy and
> [`CHANGELOG.md`](./CHANGELOG.md) for what changed per version.

## Pin policy

- `latest-amd64` / `latest-arm64` on the pgck-web repo always track the most recent published tag (whether attested or not).
- There is no `latest` on the extension OCI artifact — pin by `pg`×`arch` explicitly.
- Tagged versions are immutable on GHCR.
- From the first attested release forward, every entry on this page ships with a verifiable **SLSA Build Provenance v1** attestation tying it to a specific GitHub Actions workflow run on this repo.
