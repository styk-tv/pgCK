# Pinned Python dependencies for the aggregated `web_demo/` runtime
# and the pytest-based verification surface shipped in v0.1.2.
anyio==4.9.0
fastapi==0.116.0
httpx==0.28.1
nats-py==2.12.0
pydantic==2.11.7
pytest==8.4.1
pytest-asyncio==1.3.0
starlette==0.46.2
uvicorn==0.35.0
